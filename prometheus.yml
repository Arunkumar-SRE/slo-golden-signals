# my global config
global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:

# Load rules
rule_files:

# Scrape configurations
scrape_configs:

  # Prometheus scrapes itself
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Our demo app
  - job_name: "ecommerce-checkout"
    static_configs:
      - targets: ["localhost:8001"]
    scrape_interval: 10s
```
